

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

Widget loadingWidget() => Center(child: CircularProgressIndicator(),);

Widget noMoreWidget() => Center(child: Text('There is no more info.'),);