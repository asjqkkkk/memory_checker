name: memory_checker
description: To finder memory leak by Dart VM Service。Support for Windows/Linux/Android/iOS, not support for Web.
version: 0.0.1
verified publisher:
  - agedchen@gmail.com
homepage: https://github.com/asjqkkkk/memory_checker

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  isolate: ^2.0.3
  vm_service: ^5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: